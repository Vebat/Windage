
Speed vector {
 Angle // Угол вектора скорости относительно поверхности земли
 Module // Модуль скорости
}

DecomposingVector {
 VectorX // Составляющая вектора по оси x
 VectorY // Составляющая вектора по оси y
}

	Применение ускорения с течением времени на состовляющую вектора скорости по оси y.
	
	Расчет расстояния до земли в каждый момент времени (соразмерно точности вычислений) 
	if (distanceToEarth < 0){
	break;
	{
	
	const g = 9.8 // Ускорение свободного падения
	distanceToEarth 
	distance // Дистанции по оси х по плоскости
	
	S = V0 * t + a(t)2/2 // Формула для расчета вертикального расстояния (необходима для расчета времени касания тела с Землёй )
	t = V0/g * 2
	S = V * t // Горизонтальное расстояние
	